#pragma once
#include <stdio.h>

using namespace std;

//p = plus
//m = moins
//f = fleche
typedef enum{
  p,
  pp,
  m,
  mm,
  f,
  ff
}Symbole;

typedef enum{
  bleu, 
	vert, 
	rouge, 
	jaune, 
	violet,
  multicolor
}Couleur;